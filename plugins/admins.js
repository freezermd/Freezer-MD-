module.exports={name:'admins',category:'Group',aliases:['adminlist'],description:'List admins',async execute(sock,m,args){if(!m.isGroup)return m.reply('❌ This command is only for groups.');if(!m.isAdmin&&!m.isOwner)return m.reply('❌ Admin permission required.'); const g=await sock.groupMetadata(m.from); const a=g.participants.filter(p=>p.admin); return m.reply('🛡️ GROUP ADMINS\n\n'+a.map((p,i)=>(i+1)+'. @'+p.id.split('@')[0]).join('\n'),{mentions:a.map(p=>p.id)});}};
